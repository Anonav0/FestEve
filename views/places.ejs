<%- include('./partials/header') -%> 

<h1>This is the places page!</h1>

<a href="/places/new">Add new Puja</a>

<% places.forEach((place) => { %>
    <div>
        <h4><%= place.name %></h4>
        <img src="<%= place.image %> ">
    </div>
        


<% }); %>

<%- include('./partials/footer') -%> 